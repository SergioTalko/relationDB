-- SELECT  DIRECTOR, COUNT(TITLE) FROM MOVIE
-- GROUP BY (DIRECTOR);

-- SELECT DIRECTOR,sum(DOMESTIC_SALES), sum(INTERNATION_SALES) FROM MOVIE INNER JOIN MOVIE_STAT on ID = MOVIE_ID
-- GROUP BY (DIRECTOR);

-- SELECT TITLE,YEAR FROM MOVIE
-- ORDER BY (YEAR) DESC ;

SELECT * FROM MOVIE_STAT
ORDER BY (INTERNATION_SALES) DESC ;